################################################################################
#
# This file is autogenerated, do not edit!
#
################################################################################

config.klee_src_root = "/home/namrata/klee"
config.klee_obj_root = "/home/namrata/build"
config.klee_tools_dir = "/home/namrata/build/bin"
config.llvm_tools_dir = "/usr/lib/llvm-10/bin"

config.libcxx_include_dir = "/home/namrata/libc++-install-10/include/c++/v1/"

# Needed to check if a hack needs to be applied
config.llvm_version_major = "10"
config.llvm_version_minor = "0"

# Compilers
config.clang = "/usr/lib/llvm-10/bin/clang -I/home/namrata/klee/include"
config.clangxx = "/usr/lib/llvm-10/bin/clang++ -I/home/namrata/klee/include"

config.cc = "/usr/bin/cc  -Wall -Wextra -Wno-unused-parameter -I /home/namrata/klee/include"
config.cxx = "/usr/bin/c++  -Wall -Wextra -Wno-unused-parameter -I /home/namrata/klee/include"

# NOTE: any changes to compiler flags also have to be applied to
#       test/Concrete/CMakeLists.txt
config.O0opt = "-O0"
if 10 >= 5:
  config.O0opt += " -Xclang -disable-O0-optnone"

# Features
config.enable_uclibc = True if 1 == 1 else False
config.enable_posix_runtime = True if 1 == 1 else False
config.enable_libcxx = True if 1 == 1 else False
config.have_selinux = True if 0 == 1 else False
config.enable_stp = True if 1 == 1 else False
config.enable_z3 = True if 1 == 1 else False
config.enable_zlib = True if 1 == 1 else False
config.have_asan = True if 0 == 1 else False
config.have_ubsan = True if 0 == 1 else False
config.have_msan = True if 0 == 1 else False

# Add sanitizer list
config.environment['LSAN_OPTIONS'] = "suppressions=/home/namrata/klee/utils/sanitizers/lsan.txt"
config.environment['UBSAN_OPTIONS'] = "suppressions=/home/namrata/klee/utils/sanitizers/ubsan.txt"

# Current target
config.target_triple = "x86_64-pc-linux-gnu"

# Path to libkleeRuntest
config.libkleeruntest = "/home/namrata/build/lib/libkleeRuntest.so"

# Let the main config do the real work.
try:
  lit
except NameError:
  # Use lit_config class
  lit_config.load_config(config, "/home/namrata/klee/test/lit.cfg")
else:
  # Use old lit class
  lit.load_config(config, "/home/namrata/klee/test/lit.cfg")
